const Breadcrumb: React.FC = () => (
  <nav className="flex items-center gap-2 text-xs font-medium text-neutral-400 mb-3 tracking-widest uppercase">
    <span>Compliance Registry</span>
    <span className="text-neutral-300">›</span>
    <span className="text-teal-600 font-semibold">Add New Filing</span>
  </nav>
);

export default Breadcrumb;
